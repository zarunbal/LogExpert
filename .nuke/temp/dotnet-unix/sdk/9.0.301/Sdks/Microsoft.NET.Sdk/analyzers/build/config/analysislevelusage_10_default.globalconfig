# NOTE: Requires **VS2019 16.7** or later

# 'Usage' Rules from '10.0' release with 'Default' analysis mode
# Description: 'Usage' Rules with enabled-by-default state from '10.0' release with 'Default' analysis mode. Rules that are first released in a version later than '10.0' are disabled.

is_global = true

global_level = -99

