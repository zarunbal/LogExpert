# NOTE: Requires **VS2019 16.7** or later

# 'Documentation' Rules from '10.0' release with 'Minimum' analysis mode escalated to 'error' severity
# Description: 'Documentation' Rules with enabled-by-default state from '10.0' release with 'Minimum' analysis mode. Rules that are first released in a version later than '10.0' are disabled. Enabled rules with 'warning' severity are escalated to 'error' severity to respect 'CodeAnalysisTreatWarningsAsErrors' MSBuild property.

is_global = true

global_level = -99

